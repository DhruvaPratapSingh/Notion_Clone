import { Button } from "@/components/ui/button";
import React from "react";

const page = () => {
  return (
    <div>
      <Button variant="secondary">Motion</Button>
    </div>
  );
};

export default page;
